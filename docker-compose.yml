version: '3'
services:
    Qlinux: 
        build: 
            context: .
            dockerfile: Dockerfile
            args:
                - INSTALL_SOFTWARES=fasle
                - PRIVOXY_PORT=8998
        image: qlinux
        ports:
            - "8998:8998"
        volumes:
            - "~/.Qdotfiles/:/home/qiangzibro/.Qdotfiles/"
